# Diário de Operação
Data: 2026-03-01

### O que aconteceu hoje?
- Aprimorado o Zoom Interativo do D3.js Map.
- Implementado sistema de restrição visual e algorítmica de **Pré-requisitos** nas `Competency` com cadeia de bloqueios (`requiresIds`).
- Corrigida e habilitada configuração do Firebase (Firestore Rules + environment) e resolvida falha de permissão no painel do banco (`allow read, write: if true` para debug, logo depois revertido para as Rules seguras do app).
- Habilitada autenticação Google Popup.
- Adicionado FAB Button pro Notebook LM no canto inferior, renomeado para "✨ Chat Interativo".
- Incorporamos e documentamos as novas exigências estritas das **Práticas Angular** vindas do `AGENTS.md`. A forma antiga de compor `@Input()` e Component APIs está legada, e as Signals (`input()`, `output()`) e Native Control Flows devem guiar o desenvolvimento de forma exclusiva da Prioridade 2 em diante.

### Próximo Foco
1. Prioridade 2: "✨ Explicar com IA", onde usaremos serverless function pra acessar LLM model.
2. Refatorações e criações baseadas nos moldes do **Angular Atualizado** (sem `standalone: true`, Native Flow, Signals puristas, OnPush) em todos os passos futuros.
